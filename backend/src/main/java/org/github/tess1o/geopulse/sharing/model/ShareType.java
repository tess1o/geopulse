package org.github.tess1o.geopulse.sharing.model;

/**
 * Enum representing the type of shared link.
 */
public enum ShareType {
    /**
     * Live location share - shows current location with optional history
     */
    LIVE_LOCATION,

    /**
     * Timeline share - shows historical timeline data for a specific date range
     */
    TIMELINE
}
