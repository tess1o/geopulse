# GeoPulse k6 Load Testing - Environment Configuration
#
# IMPORTANT: k6 does NOT read .env files automatically!
# This file is for documentation/reference only.
#
# To use these variables, you must:
# 1. Export them in your shell: export BASE_URL=https://...
# 2. Pass inline: BASE_URL=https://... k6 run script.js
# 3. Use -e flag: k6 run -e BASE_URL=https://... script.js
#
# You can copy this file to .env for your own reference:
# cp .env.example .env

# Production server URL (REQUIRED)
# Example: https://geopulse.example.com
BASE_URL=https://your-production-server.com

# Number of test users (must match the number created in SQL setup)
# Valid range: 1-100
# Default: 20
NUM_TEST_USERS=20

# Password for test users
# This should match the password used when creating test users
# If using source user's password hash, use the same password here
# Default: LoadTest123!
TEST_USER_PASSWORD=LoadTest123!

# Test data date ranges
# IMPORTANT: Set these to match the actual GPS data date range in your production database
# This ensures date range queries stay within the bounds of your test data
#
# To find your test data dates, run this SQL query against your production database:
# SELECT MIN(timestamp) as start_date, MAX(timestamp) as end_date
# FROM gps_points
# WHERE user_id IN (SELECT id FROM users WHERE email LIKE 'loadtest-user-%@example.com');
#
# Example values (update with your actual dates):
TEST_DATA_START_DATE=2025-05-01T00:00:00.000Z
TEST_DATA_END_DATE=2026-05-20T23:59:59.999Z

# Load multiplier for scaling virtual users (VUs)
# Default: 1 (25 concurrent VUs total)
# Examples:
#   LOAD_MULTIPLIER=1  → 25 VUs  (10/7/5/3)
#   LOAD_MULTIPLIER=2  → 50 VUs  (20/14/10/6)
#   LOAD_MULTIPLIER=4  → 100 VUs (40/28/20/12)
#   LOAD_MULTIPLIER=10 → 250 VUs (100/70/50/30)
LOAD_MULTIPLIER=1
