apiVersion: v1
kind: Service
metadata:
  name: {{ include "geopulse.fullname" . }}-backend
  labels:
    {{- include "geopulse.backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "geopulse.backend.selectorLabels" . | nindent 4 }}